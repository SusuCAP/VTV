{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal local media capabilities for the VTV control client",
  "windows": ["main"],
  "permissions": ["core:default"]
}
