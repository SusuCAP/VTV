[project]
name = "vtv-schemas"
version = "0.1.0"
description = "Shared API and worker contracts for VTV"
requires-python = ">=3.12"
dependencies = ["pydantic>=2.10.6"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
