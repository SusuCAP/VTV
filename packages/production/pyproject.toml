[project]
name = "vtv-production"
version = "0.1.0"
description = "Localization, dubbing and lipsync production contracts for VTV"
requires-python = ">=3.12"
dependencies = [
  "pydantic>=2.10.6",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
