[project]
name = "vtv-analysis"
version = "0.1.0"
description = "Stable analysis adapter contracts and deterministic reference adapters for VTV"
requires-python = ">=3.12"
dependencies = ["pydantic>=2.10.6"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
